# YouTube to GIF Converter セットアップガイド

GUIで時間と領域を指定してYouTube動画をGIFに変換するツールです。

## 必要なソフトウェア

### 1. Python 3.7 以上
[python.org](https://www.python.org/downloads/) からダウンロード

### 2. FFmpeg と FFprobe
動画処理に必須

**Windows:**
```bash
# Chocolateyを使用
choco install ffmpeg

# または、以下のサイトからダウンロード
# https://ffmpeg.org/download.html
```

**macOS:**
```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install ffmpeg
```

### 3. yt-dlp (YouTube動画ダウンロード用)
```bash
pip install yt-dlp
# または
pip install youtube-dl
```

## インストール手順

### 1. 必要なPythonパッケージのインストール
```bash
# tkinterは通常Python に含まれています
# 追加パッケージが必要な場合:
pip install pillow  # 画像処理用
```

### 2. スクリプトの実行
```bash
python youtube_to_gif.py
```

## 使用方法

### タブ 1: ダウンロード & プレビュー
- YouTubeの URL を貼り付けて「ダウンロード」をクリック
- 動画情報 (長さ、解像度) が自動的に表示されます

### タブ 2: 時間を選択
- **開始時刻**: GIF化する範囲の開始地点 (秒)
- **終了時刻**: GIF化する範囲の終了地点 (秒)
- 例: 5秒から15秒を抽出 → 開始: 5, 終了: 15

### タブ 3: 領域を選択
GIFで切り取る画面領域を指定します

**プリセット:**
- **全体**: 元の解像度のまま (1920x1080)
- **中央**: 中央部分を抽出 (960x540)
- **上部**: 画面上部を抽出 (1920x540)

**カスタム指定:**
- x, y: 左上角の座標
- width, height: 抽出する幅と高さ
- 例: 左から480px、上から270pxの位置から960x540の領域を抽出

### タブ 4: GIFに変換
変換設定を指定して実行

**パラメータ:**
- **FPS**: フレームレート (デフォルト: 10)
  - 値が高い = スムーズだが ファイル size が大きい
  - 値が低い = ファイル size が小さいが途切れたように見える
  - 推奨: 10 fps
  
- **色数**: GIF内の色の数
  - 16: 最小 (低品質, 小 size)
  - 64-128: 中程度 (バランス型)
  - 256: 最大 (高品質)

- **スケール**: 出力画像のサイズ
  - 0.5: 半分のサイズ (ファイル size 大幅削減)
  - 1.0: 元のサイズ
  - 2.0: 2倍のサイズ

- **出力先**: 保存するファイルパス

## 使用例

### 例 1: YouTubeクリップを小さなGIFに
1. URL をダウンロード
2. 時間: 10秒～15秒
3. 領域: 中央 (960x540)
4. 設定:
   - FPS: 10
   - 色数: 128
   - スケール: 0.5
   - 出力: `memes.gif`

### 例 2: ゲームプレイのキャプチャ
1. URL をダウンロード
2. 時間: 30秒～60秒
3. 領域: 全体 (1920x1080)
4. 設定:
   - FPS: 12
   - 色数: 256
   - スケール: 1.0
   - 出力: `gameplay.gif`

## トラブルシューティング

### エラー: "ffmpeg not found"
→ FFmpeg がインストールされていません
```bash
# Windowsの場合
choco install ffmpeg

# Macの場合
brew install ffmpeg
```

### エラー: "yt-dlp not found"
→ YouTube ダウンロードツールをインストール
```bash
pip install yt-dlp
```

### GIFが大きすぎる
→ 以下の対策を試してください
- スケール: 0.5 または 0.75 に設定
- FPS: 5～8 に低下
- 色数: 128 または 64 に減少
- 時間範囲: より短くする

### GIFが低品質
→ 以下で改善できます
- FPS: 15～20 に増加
- 色数: 256 に設定
- スケール: 1.0 に設定

## コマンドラインでの使用 (オプション)

ffmpegコマンドを直接使うこともできます:

```bash
# 基本的な使用方法
ffmpeg -ss 10 -t 5 -i video.mp4 -vf "crop=960:540:480:270,fps=10" output.gif

# パラメータ説明:
# -ss 10          : 10秒から開始
# -t 5            : 5秒間の動画
# -i video.mp4    : 入力ファイル
# crop=W:H:X:Y    : 幅:高さ:x座標:y座標
# fps=10          : 10フレーム/秒
```

## ファイルサイズの目安

| 解像度 | 時間 | FPS | 色数 | スケール | 目安 size  |
|--------|------|-----|------|---------|-----------|
| 1920x1080 | 5秒 | 10 | 256 | 1.0 | 10-20MB |
| 960x540 | 5秒 | 10 | 128 | 1.0 | 2-5MB |
| 960x540 | 5秒 | 10 | 128 | 0.5 | 0.5-1.5MB |
| 1920x1080 | 10秒 | 5 | 64 | 0.5 | 3-8MB |

## よくある質問

**Q: オンラインでGIFに変換できるサイトはありますか?**
A: はい、いくつかありますが、ローカルでの処理の方が速く、プライバシーも保たれます。

**Q: MP4やWebmなど他の動画形式も対応していますか?**
A: はい、FFmpegが対応している形式なら大丈夫です (MP4, WebM, MKV など)

**Q: バッチ処理 (複数動画を一度に変換) は?**
A: スクリプトをカスタマイズすれば可能です。必要に応じてお知らせください。

**Q: 高品質のGIFにするには?**
A: FPS を 15～20, 色数を 256, スケールを 1.0 に設定してください。その分ファイル size は大きくなります。

---

質問や改善案があれば、お気軽にお知らせください！
