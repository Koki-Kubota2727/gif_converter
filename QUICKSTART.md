# 🚀 5分で始める YouTube to GIF Converter

## ステップ 1: 環境準備 (2分)

### Windows ユーザー
```bash
# PowerShell または コマンドプロンプトを開く

# 1. Chocolateyがあれば (ない場合は https://chocolatey.org/install から)
choco install ffmpeg yt-dlp python

# 2. Pythonパッケージをインストール
python -m pip install yt-dlp pillow

# または、セットアップスクリプトを実行
python setup.py
```

### macOS ユーザー
```bash
# ターミナルを開く

# 1. Homebrewをインストール (https://brew.sh/)
brew install ffmpeg
brew install python

# 2. Pythonパッケージをインストール
pip3 install yt-dlp pillow

# または
python3 setup.py
```

### Linux ユーザー
```bash
# Ubuntu/Debian の場合
sudo apt-get update
sudo apt-get install ffmpeg python3-pip
pip3 install yt-dlp pillow

# または
python3 setup.py
```

## ステップ 2: アプリを起動 (1分)

```bash
# スクリプトがあるディレクトリで実行
python youtube_to_gif.py

# または
python3 youtube_to_gif.py
```

GUIウィンドウが開きます！

## ステップ 3: 動画をダウンロード (1分)

1. **YouTubeのURL をコピー**
   - YouTube で好きな動画を開く
   - URL (例: `https://www.youtube.com/watch?v=...`) をコピー

2. **アプリに貼り付け**
   - アプリの「ダウンロード & プレビュー」タブを開く
   - URL をテキストボックスに貼り付け
   - 「ダウンロード」をクリック
   - ダウンロード完了時に動画情報が表示されます

## ステップ 4: GIF化する範囲を指定 (1分)

### 「時間を選択」タブ
- **開始時刻**: GIF化する部分の開始地点 (秒)
- **終了時刻**: GIF化する部分の終了地点 (秒)
- 例: `5秒` から `10秒` を抽出したい場合
  - 開始: 5
  - 終了: 10

### 「領域を選択」タブ
どの部分を切り取るかを選択 (オプション)
- **プリセット使用** (簡単)
  - 「全体」: 元の画面をそのまま使用
  - 「中央」: 中央部分を抽出
  - 「上部」: 画面上部を抽出
- **カスタム指定** (詳細制御)
  - x, y, width, height を数値で指定
  - 例: 左から 480px、上から 270px の位置から 960x540 を抽出

## ステップ 5: GIFに変換 (1分)

### 「GIFに変換」タブ

**推奨設定 (バランス型)**
- **FPS**: 10
- **色数**: 128
- **スケール**: 1.0 (ファイルサイズを小さくしたければ 0.5)
- **出力先**: 自動設定 (変更可能)

「GIFに変換」ボタンをクリック！
- 処理中はプログレスバーが表示されます
- 完了時にファイルパスが表示されます

## よく使うプリセット

### 🎥 SNS用 (小さく軽い)
- FPS: 8-10
- 色数: 128
- スケール: 0.5
- → ファイルサイズ: 1-3MB

### 🎮 ゲームクリップ (高品質)
- FPS: 12-15
- 色数: 256
- スケール: 1.0
- → ファイルサイズ: 10-30MB

### 📱 モバイル用 (小さく高速)
- FPS: 5-8
- 色数: 64
- スケール: 0.75
- → ファイルサイズ: 0.5-1.5MB

### 🎬 高品質保存用
- FPS: 20
- 色数: 256
- スケール: 1.5-2.0
- → ファイルサイズ: 30-100MB

## 実践例

### 例 1: TikTok用の短いクリップ (5秒)
1. YouTube URL をダウンロード
2. 時間: 30秒 ～ 35秒
3. 領域: 「全体」
4. 設定: FPS 8, 色数 128, スケール 0.5
5. 出力: `tiktok_clip.gif`

### 例 2: Discordで共有する反応動画
1. YouTube URL をダウンロード
2. 時間: 20秒 ～ 25秒
3. 領域: 「中央」
4. 設定: FPS 12, 色数 256, スケール 0.75
5. 出力: `reaction.gif`

### 例 3: ゲーム実況のハイライト
1. URL をダウンロード
2. 時間: 1分 30秒 ～ 2分
3. 領域: カスタム (好きな部分)
4. 設定: FPS 15, 色数 256, スケール 1.0
5. 出力: `gaming_highlight.gif`

## トラブル対応

### GIFが作成されない
- ✅ FFmpeg がインストールされているか確認
  ```bash
  ffmpeg -version
  ```
- ✅ YouTube URL が正しいか確認
- ✅ 時間指定が正しいか確認 (開始 < 終了)

### ファイルサイズが大きい
→ 以下のいずれかを試す:
- **スケール** を 0.5 に (サイズ 1/4 に)
- **FPS** を 5-8 に低下
- **色数** を 64-128 に低下
- **時間範囲** をもっと短く

### 品質が悪い
→ 以下のいずれかを試す:
- **FPS** を 15-20 に増加
- **色数** を 256 に変更
- **スケール** を 1.0 に変更

### インストール時にエラー
```bash
# 管理者権限で再度実行してみる
# または手動でインストール
python -m pip install --upgrade pip
python -m pip install yt-dlp pillow
```

## 便利なTips

💡 **複数の GIF を作成する場合**
- アプリを再度起動する必要はありません
- 設定を変えるだけで、何度でも変換可能

💡 **ローカルファイルも変換可能**
- `youtube_to_gif.py` を以下のように改造すれば、ローカル動画も処理可能
- フォルダ選択機能を追加可能

💡 **コマンドラインで一括処理したい場合**
```bash
# ffmpeg を直接使用
ffmpeg -ss 30 -t 5 -i video.mp4 -vf "fps=10" output.gif
```

## サポート・カスタマイズ

このスクリプトはカスタマイズ可能です！以下のような機能追加ができます:

- ローカルファイルの処理対応
- バッチ処理 (複数動画の一括変換)
- より詳細なプレビュー機能
- ドラッグ&ドロップ対応
- キーボードショートカット

必要な機能があれば、お知らせください！

---

**Happy GIF Making! 🎬✨**
